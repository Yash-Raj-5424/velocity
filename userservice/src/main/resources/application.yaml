spring:
  application:
    name: userservice
  datasource:
    url: jdbc:postgresql://localhost:5433/velocity-micro-user
    username: postgres
    password: mishGill

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
server:
  port: 8081